# PhoneMarket — Frontend (Next.js)

> **Status**: Placeholder — Next.js project will be scaffolded here.

## Quick Start

```bash
npx -y create-next-app@latest ./ --ts --eslint --app --src-dir --tailwind --import-alias "@/*"
npm run dev
```

The frontend will connect to the backend API at `http://localhost:8000/api/v1`.
